import { defineConfig } from 'vite'

export default defineConfig({
  // FIXME: 請將 'YOUR_REPO_NAME' 換成你即將在 GitHub 上建立的專案名稱 (Repository Name)
  // 例如你的專案叫做 sel-community，就改成 '/sel-community/'
  base: '/YOUR_REPO_NAME/',
})
